defaults:
  - model: base
  - data: default
  - trainer: default
  - optimizer: adamw
  - logger: mlflow
  - _self_

seed: 42

hydra:
  job:
    chdir: false
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
