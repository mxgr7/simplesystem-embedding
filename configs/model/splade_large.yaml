model_name: deepset/gbert-large
architecture: splade
init_checkpoint: /home/max/prod_splade/large_pretrain.ckpt  # =-free symlink -> checkpoints/splade-large-pretrain/best-step=4688...=0.6423.ckpt (Hydra = trap); domain-pretrained large SPLADE warm start
output_dim: null
pooling: splade_max
loss_type: contrastive
similarity_scale: 1.0
triplet_margin: 0.2
triplet_negative_selection: semi_hard
gradient_checkpointing: true
teacher_checkpoint: null
distill_temperature: 0.05
compile: false
flops_lambda_q: 5.0e-4
flops_lambda_d: 3.0e-4
flops_warmup_steps: 1500
loss_weights:
  margin_mse: 0.0
  kl: 0.0
