.venv/
.ruff_cache/
.claude/
__pycache__/
*.pyc

.pytest_cache/
.cache/

outputs/
checkpoints/
mlruns/
mlruns-smoke/

mlflow.db

logs/

# Local-only 6GB mmap lookup cache (symlink to /data); rebuilt by the v5 importer.
lookup_cache

.env
.nextgen-auth
.nextgen-auth.tmp
.opencode/
.vscode/

# DuckDB ↔ Python parity diff dumps.
tests/.parity_diff*.json
tests/.aggregate_parity_diff*.json

# Heavy local-only fixture; rebuild from S3 cache via scripts/dump_s3_sample.py.
tests/fixtures/mongo_sample/sample_10k.json

# HNSW benchmark artifacts — large generated parquet/npy + per-run bench JSON.
# Rebuild via scripts/build_hnsw_eval_dataset.py + scripts/build_full_corpus_oracle.py.
reports/hnsw_eval*/

.obsidian/
