[project]
name = "aws-embedding-bench"
version = "0.1.0"
description = "Benchmark TEI throughput across AWS GPU/CPU/Inferentia instance types"
requires-python = ">=3.10"
dependencies = [
    "pulumi>=3.130.0",
    "pulumi-aws>=6.50.0",
]

[tool.uv]
package = false
