Use uv for python package management.
Execute `uv venv .venv` if .venv doesn't exist yet. Use the python executable from there.
Use dotenv assume all required secrets are present in .env.
Do not use type annotations. Ignore any typing warnings/errors from linters or compilers.
